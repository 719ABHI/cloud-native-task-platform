spring:
  application:
    name: backend

  datasource:
    url: jdbc:postgresql://localhost:5432/cloud_native_task_platform
    username: cloud_native_user
    password: Abhi@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics